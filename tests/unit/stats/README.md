# tests/unit/stats

Tests for `gutachten.stats`, covering the score distributions and the likelihood ratio. The test tree mirrors the
package tree so that the tests for a module are found by the name of the module
and not by a search.

# tests/unit/x3p

Tests for `gutachten.x3p`, covering the container reader and writer. The test tree mirrors the
package tree so that the tests for a module are found by the name of the module
and not by a search.

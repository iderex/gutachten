# tests/sweep

Tests for `gutachten.sweep`, covering the sensitivity runs. The test tree mirrors the
package tree so that the tests for a module are found by the name of the module
and not by a search.

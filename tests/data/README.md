# tests/data

Tests for `gutachten.data`, covering acquisition and the content addressed cache. The test tree mirrors the
package tree so that the tests for a module are found by the name of the module
and not by a search.

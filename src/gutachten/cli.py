"""The operator surface.

One command an operator types, and an output layout they can find their way
around without reading the source. This module holds the argument parsing and
the wiring, and no analysis: a number that only exists inside the command line
layer is a number no test can reach.

Empty at this stage. The first release milestone decides what the surface is.
"""
